import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#e3e3e3",
    },
    secondary: {
      main: "#5e6e7d",
    },
    background: {
      default: "#e3e3e3",
    },
  },
});

export default theme;
